import React from 'react';
import Clock from './clock';

export default props => (
    <div className="container">
        <h1 className="text-center">Redux prototype</h1>
        <Clock/>
    </div>
)

